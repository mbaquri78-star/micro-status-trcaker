# Micro-Status Tracker

A Streamlit app to capture real-time project updates.

## Run
cd micro_status_tracker
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
streamlit run app.py
